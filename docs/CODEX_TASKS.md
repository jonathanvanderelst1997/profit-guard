# Codex task list

1. Scaffold official Shopify React Router app.
2. Copy this MVP pack into the generated app.
3. Run `npm install` and `npm run typecheck`.
4. Run `npx prisma migrate dev --name profit_guard_core`.
5. Install on dev store with sample products and costs.
6. Test: missing cost, low margin, loss, supplier CSV preview, export CSV, uninstall webhook.
7. Prepare App Store screenshots and submit review.
